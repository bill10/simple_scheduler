# Authentication credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password

# Application secret key (used for session encryption)
# You can generate a secure key with: python -c "import secrets; print(secrets.token_hex(24))"
SECRET_KEY=replace_with_your_secure_secret_key
